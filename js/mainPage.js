// JavaScript source code
$(document).ready(function () {
    //clear local memory
    //sessionStorage.clear();
    sessionStorage.removeItem("percents");
    sessionStorage.removeItem("scores");
    sessionStorage.removeItem("units");
    sessionStorage.removeItem("grades");
});